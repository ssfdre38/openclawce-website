---
/**
 * Container - Centered content wrapper with max-width
 */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["mx-auto max-w-6xl px-6", className]}>
  <slot />
</div>
